import React from 'react'
import ReactDOM from 'react-dom/client'
import QueryMind from './QueryMind.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <QueryMind />
  </React.StrictMode>
)
