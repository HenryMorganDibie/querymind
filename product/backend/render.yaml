services:
  - type: web
    name: querymind-backend
    runtime: python
    rootDir: product/backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    plan: free
