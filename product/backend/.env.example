# No secrets needed here — users supply their own API keys via the frontend.
# Railway will set PORT automatically.
# If you want to lock CORS to your Vercel domain, set this:
ALLOWED_ORIGIN=https://your-app.vercel.app
